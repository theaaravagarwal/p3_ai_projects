[project]
name = "assistant-bot"
version = "0.1.0"
description = "AI-powered general-purpose chatbot built with OpenAI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.109.1",
    "python-dotenv>=1.2.2",
]
