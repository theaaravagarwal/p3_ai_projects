[project]
name = "assistant-bot"
version = "0.1.0"
description = "AI-powered general-purpose chatbot built with Langflow"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langflow>=1.8.4",
    "openai>=1.109.1",
    "python-dotenv>=1.2.2",
]
