# Minimal runtime dependencies for the Docker deploy path.
# This intentionally excludes Langflow to keep the image small.

openai>=1.109.1
python-dotenv>=1.2.2
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
